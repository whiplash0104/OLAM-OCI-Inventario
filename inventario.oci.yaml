---
plugin: oci

regions:
  - "{{ region }}"

compartments: 
  - compartment_ocid: "{{ compartment_id }}"
    fetch_compute_hosts: True

hostname_format_preferences:
  - "display_name"
  - "hostname_label"
  - "public_ip"
  - "private_ip"
